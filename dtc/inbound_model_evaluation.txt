INBOUND MATCH MODEL EVALUATION
============================================================

Training label: strict pseudo-labels from high-confidence single-row inbound matches.
No deterministic inbound ground-truth key exists in the current data.

Pseudo-label rule:
  top candidate, score >= 85.0, margin >= 10.0,
  date abs days <= 1,
  weight diff <= max(1.0, 0.500% * DTC weight)

Candidate rows: 1945
DTC orders: 85
Pseudo-labeled orders used for training/evaluation: 54/85
Pseudo-positive candidate rows: 54

GROUPED 5-FOLD CROSS-VALIDATION BY DTC ROW
  Evaluated only against pseudo-labeled orders.
  Rule-score top-1 accuracy vs pseudo-labels: 54/54 (100.0%)
  Model top-1 accuracy vs pseudo-labels:     54/54 (100.0%)

FINAL MODEL FIT ON PSEUDO-LABELED CANDIDATES
  In-sample top-1 accuracy vs pseudo-labels: 54/54 (100.0%)

Caveat: these metrics measure agreement with automated pseudo-labels,
not independent inbound truth.

Files written:
  inbound_training_candidates_full.csv
  inbound_training_features.csv
  inbound_pseudo_labels.csv
  trained_inbound_scored_candidates.csv
  trained_inbound_matches.csv
  inbound_match_model.json
